# Output (local preview only — never commit)
output/

# Dashboard runtime data (SQLite DB, etc.)
data/

# Environment & secrets
.env
config/secrets.py
config/agents.csv          # roster — contains real names + emails
deploy/cloudflared.yml     # rendered tunnel config (carries credentials)

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db
