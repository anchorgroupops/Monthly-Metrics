# Output (local preview only — never commit)
output/

# Dashboard runtime data (SQLite DB, etc.)
data/

# Environment & secrets
.env
config/secrets.py
# Roster — contains real names + emails
config/agents.csv
# Rendered tunnel config (carries credentials)
deploy/cloudflared.yml

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db
